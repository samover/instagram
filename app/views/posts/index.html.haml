= link_to 'Add photo', new_post_path 
- if @posts.any?
  - @posts.each do |post|
    .post-container
      %p.time_ago= "#{time_ago_in_words post.created_at} ago"
      %p.username= email_to_username(post.user_id)
      = image_tag post.image(:medium)
      %p.caption= post.caption
- else
  %h2 No photos posted yet
